---
- hosts: localhost
  remote_user: root
  tasks:
  - name: Create Repo-Directory
    file:
     path: "{{ Directory_Name }}"
     satte: directory
  - name: clone the repo
    ansible.builtin.git:
     repo: "{{ URL }}"
     dest: "{{ Directory_Name }}"
  - name: Create MANIFEST.in file
    blockinfile:
     path: "{{ Directory_Name }}/MANIFEST.in"
     create: true
     block: |
      "{{ Content }}"
  - shell: git add .
  - shell: git commit -m "Project Signing done"
  - shell: git push     
   
